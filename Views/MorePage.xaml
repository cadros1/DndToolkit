<Page x:Class="DnDToolkit.Views.MorePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DnDToolkit.Views"
      xmlns:vm="clr-namespace:DnDToolkit.ViewModels"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=vm:MoreViewModel}"
      Title="MorePage">

    <Grid Margin="30">
        <StackPanel>
            <TextBlock Text="更多内容" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

            <!-- 资源按钮 -->
            <ui:CardAction Icon="{ui:SymbolIcon Library24}" 
                           Command="{Binding GoToResourcesCommand}">
                <StackPanel>
                    <TextBlock Text="资源中心" FontWeight="SemiBold"/>
                    <TextBlock Text="下载规则书、角色卡模板等文件" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12"/>
                </StackPanel>
            </ui:CardAction>
        </StackPanel>
    </Grid>
</Page>
