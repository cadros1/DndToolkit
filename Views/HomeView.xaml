<Page x:Class="DnDToolkit.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DnDToolkit.Views"
      xmlns:vm="clr-namespace:DnDToolkit.ViewModels"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" 
      d:DataContext="{d:DesignInstance Type=vm:HomeViewModel}"
      d:Height="450" d:Width="800"
      mc:Ignorable="d"
      Tag="HomeView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Label Content="Welcome to DnD Toolkit!"
               Grid.Row="0"
               Padding="20"
               HorizontalAlignment="Center" 
               VerticalAlignment="Bottom"
               HorizontalContentAlignment="Center"
               VerticalContentAlignment="Center"/>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <ui:Button Grid.Row="0" 
                       Content="获取本程序使用的角色卡PDF"
                       Appearance="Primary"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Top"/>

            <ui:ProgressRing Grid.Row="1"
                             VerticalAlignment="Top"
                             HorizontalAlignment="Center"
                             IsIndeterminate="True"
                             Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Page>
