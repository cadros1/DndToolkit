<Page x:Class="DnDToolkit.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:DnDToolkit.Views"
      xmlns:vm="clr-namespace:DnDToolkit.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel}"
      Title="DashBoardPage">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
            <ui:SymbolIcon Symbol="Home24" FontSize="60" />

            <TextBlock Text="欢迎使用 DnD 工具箱" 
                       FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center"/>

            <TextBlock Text="创建、管理并导出你的 5E 角色卡" 
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"/>

            <ui:Button Content="下载本应用使用的角色卡 PDF" 
                       Icon="{ui:SymbolIcon ArrowDownload24}"
                       Appearance="Primary"
                       Command="{Binding DownloadPdfCommand}"
                       HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Page>
