<ui:FluentWindow x:Class="DnDToolkit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DnDToolkit"
        xmlns:views="clr-namespace:DnDToolkit.Views"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:viewmodels="clr-namespace:DnDToolkit.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="750" Width="1400"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" 
                     Title="{Binding ApplicationTitle}"/>

        <ui:NavigationView Grid.Row="1"
                           x:Name="RootNavigation"
                           PaneDisplayMode="Left"
                           IsBackButtonVisible="Collapsed"
                           IsPaneOpen="False">

            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="角色卡" 
                                       Icon="{ui:SymbolIcon People24}" 
                                       TargetPageType="{x:Type views:CharacterListPage}" />
                <ui:NavigationViewItem Content="冒险"
                                       Icon="{ui:SymbolIcon Map24}"
                                       TargetPageType="{x:Type views:AdventurePage}"/>
                <ui:NavigationViewItem Content="移动设备"
                                       Icon="{ui:SymbolIcon PhoneDesktop24}"
                                       TargetPageType="{x:Type views:SyncPage}"/>
                <ui:NavigationViewItem Content="更多" 
                                       Icon="{ui:SymbolIcon MoreHorizontal24}" 
                                       TargetPageType="{x:Type views:MorePage}" />
            </ui:NavigationView.MenuItems>
        </ui:NavigationView>

        <ui:SnackbarPresenter x:Name="SnackbarPresenter" 
                              Grid.RowSpan="2" 
                              VerticalAlignment="Bottom" />
    </Grid>
</ui:FluentWindow>
